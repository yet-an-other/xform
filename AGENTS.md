# xform

## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues for this repo (`yet-an-other/xform`), using the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: one `CONTEXT.md` at the repo root + `docs/adr/`. See `docs/agents/domain.md`.

### Reporting

When reporting information to me, be extrimely concise, and sacrifice grammar for the sake of concision.